services:
  app:
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - "8081:8081"
      - "19000:19000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
    stdin_open: true
    tty: true
